<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

	<beans:import resource="classpath:/dataSource.xml" />
	<http auto-config="true">
		<!-- access denied page -->
		<access-denied-handler error-page="/403" />
		<intercept-url pattern="/dashboard*" method="GET"
			access="hasRole('USER')" />
		<intercept-url pattern="/dashboard*" method="POST"
			access="hasRole('USER')" />
		<intercept-url pattern="/addComputer*" access="hasRole('USER')" />
		<intercept-url pattern="/editComputer*" access="hasRole('USER')" />
		<http-basic />
	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service id="userService" data-source-ref="dataSource" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>
